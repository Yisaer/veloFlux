# SQL Syntax Docs

This section contains SQL-visible language semantics for StreamDialect.

- `language/`: Expression and SELECT-level semantics.
- `windows/`: Window syntax and window-specific execution semantics.
