# Runtime Extensibility

This directory contains extension mechanisms for runtime connector/codec integration.

- `registry_extensibility.md`: registry-based connector and codec extension
  model.
- `unified_distro_runtime.md`: single-entrypoint distro architecture and
  build-time distro selection model.
