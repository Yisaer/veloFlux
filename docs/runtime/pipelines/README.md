# Runtime Pipelines

This directory contains pipeline lifecycle and observability semantics.

- `lifecycle.md`: Pipeline resource lifecycle, desired-state persistence, and runtime behavior.
- `stats.md`: Pipeline processor stats scope, payload semantics, and timeout behavior.
