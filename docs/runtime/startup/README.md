# Runtime Startup

This directory contains startup-time bootstrap, apply, and cache behavior.

- `init_process.md`: Startup-only `init.json` apply flow.
- `metadata_bundle.md`: Storage snapshot model shared by import, export, and `init.json`.
