name: Bench (manual)

on:
  workflow_dispatch:
    inputs:
      ref:
        description: Git ref (branch, tag, or SHA) to benchmark
        required: true
        default: main
        type: string

jobs:
  bench:
    uses: ./.github/workflows/_bench_run.yml
    with:
      ref: ${{ inputs.ref }}

