[package]
name = "veloflux-udf-sdk"
version = "0.1.0"
edition = "2021"
description = "Helper library for writing veloFlux WASM UDFs in Rust"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
