[package]
name = "runtime_thread_cgroup_demo"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
tokio = { version = "1", default-features = false, features = ["rt-multi-thread", "sync", "time"] }

[workspace]
